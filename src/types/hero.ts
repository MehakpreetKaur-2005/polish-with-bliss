export interface HeroSlide {
  /** Stable key used for the crossfade transition and Ken Burns animation reset */
  id: string;
  /** Path to the background image, relative to /public */
  src: string;
}
